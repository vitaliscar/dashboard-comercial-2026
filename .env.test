# Test environment database URLs (dummy values for CI/E2E)
# These allow the app to start without a real database connection
DATABASE_ADMIN_URL=postgresql://admin:password@localhost:5432/test
DATABASE_URL=postgresql://app_user:password@localhost:5432/test
