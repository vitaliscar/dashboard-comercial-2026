# Test environment database URLs (dummy placeholders — no secrets reales)
# Copy to .env.test locally if needed; values below are intentionally fake.
DATABASE_ADMIN_URL=postgresql://test_admin:CHANGE_ME_TEST_ONLY@localhost:5432/test
DATABASE_URL=postgresql://test_user:CHANGE_ME_TEST_ONLY@localhost:5432/test
