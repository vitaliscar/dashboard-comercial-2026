services:
  postgres:
    image: postgres:18.6
    container_name: dashboard-comercial-postgres
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER:?falta POSTGRES_USER en .env}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?falta POSTGRES_PASSWORD en .env}
      POSTGRES_DB: ${POSTGRES_DB:-dashboard_comercial}
      APP_ADMIN_PASSWORD: ${APP_ADMIN_PASSWORD:?falta APP_ADMIN_PASSWORD en .env}
      APP_USER_PASSWORD: ${APP_USER_PASSWORD:?falta APP_USER_PASSWORD en .env}
    # Solo accesible desde localhost — Next.js corre en la misma máquina.
    # No exponer el puerto de Postgres a la red pública.
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-55432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
      - ../docker/postgres-init:/docker-entrypoint-initdb.d:ro
      # Backups automáticos: montar un volumen adicional aquí si se agrega
      # un cron de pg_dump (no incluido por defecto).
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB:-dashboard_comercial}"]
      interval: 5s
      timeout: 5s
      retries: 10
    deploy:
      resources:
        limits:
          memory: 2g
    networks:
      - internal

# Subnet propio: 172.19.0.0/16 es LAN de sucursales (p.ej. Caracas
# 172.19.6.0/24). Si Docker lo reclama, las respuestas HTTP a esas IPs
# nunca salen por el gateway y el cliente ve "tardó demasiado en responder".
networks:
  internal:
    driver: bridge
    ipam:
      config:
        - subnet: 172.28.0.0/16

volumes:
  postgres_data:
    driver: local
