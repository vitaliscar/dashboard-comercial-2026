import type { UnidadNegocioLead } from "@/lib/actions/mercadeo";

// Deshabilitado por ahora: no se necesita en la versión que va a producción.
// Implementación completa (LeadsKpis/LeadsEmbudo + getClientesPotencialesResumenAction)
// en el historial de git si hace falta reactivarla.
export function ClientesPotencialesSection(_props: { unidad: UnidadNegocioLead }) {
  return null;
}
